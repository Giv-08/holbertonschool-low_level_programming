#include <stdio.h>
#include "main.h"

/**
* mul  - Multiplies two integers
* @a: The integer in parameter
* @b: The integer in parameter
* Return: 1 if c is uppercase, 0 otherwise
*/

int mul(int a, int b)
{
	int result;
	result = a * b;
	return (result);
}
