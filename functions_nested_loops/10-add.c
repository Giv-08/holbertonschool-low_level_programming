#include <stdio.h>
#include "main.h"

/**
* add - Adds two integers 'a' abd 'b' then return the result
* @a: is an interger taken as a parameter
* @b: is an interger taken as a parameter
* Return: result of calculation
*/
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);

}
